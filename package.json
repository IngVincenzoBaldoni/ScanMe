{
  "name": "scanme",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "services/lambdas"
  ],
  "scripts": {
    "web:dev": "npm run dev --workspace @scanme/web",
    "web:build": "npm run build --workspace @scanme/web",
    "lambdas:package": "npm run package --workspace @scanme/lambdas"
  }
}
